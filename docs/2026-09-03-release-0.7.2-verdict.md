# session-handoff 0.7.2 release readiness

Date: 2026-09-03

Status: **LOCAL-READY / OWNER-GATED NO-GO**. The candidate is clean and the
local implementation gates pass. Public release remains gated by external
operations; human benchmark review is intentionally excluded from this run.

## Candidate

- Worktree: local clean release candidate
- Branch: `release/0.7.2-candidate`
- Functional revision: `00dbce4`
- Backend candidate revision checked: `2281e54` in
  `/home/cristina/selfhosted/telemetry-release-0.7.2`; the original
  `/home/cristina/selfhosted/telemetry` checkout remains preserved and dirty.

## Verified locally

- Candidate pytest: `714 passed, 2 skipped`.
- Ruff, compileall, `npm pack --dry-run --ignore-scripts`, and `git diff --check`: passed.
- Focused hardening tests: safe partial OTLP responses keep the complete batch;
  feedback preserves `origin=real`; MCP rejects malformed initialize params;
  MCP fails closed when secure relative filesystem primitives are unavailable.
- Luna xhigh found no P0. The valid findings above were independently verified
  and fixed; the reported feedback-origin issue was a false positive and is
  covered by a regression test.
- Backend pytest: `26 passed, 4 skipped`; Compose config passed; Loki reported
  ready; all five backend containers were running at inspection time.
- Cloudflare read-only inventory: `sando-telemetry` healthy, route fallback
  returns 404, and no account Logpush jobs are configured.
- Owner decisions (2026-09-03): Cloudflare edge-data/retention terms and the
  documented backup/retention policy are accepted. No Cloudflare configuration
  mutation or backup creation was performed.

## Still open

- Backend checkout contains pre-existing tracked and untracked changes; no
  reset, deletion, or blind commit was performed.
- Owner acceptance of Cloudflare edge-data/retention and the documented
  retention/no-backup policy is recorded. The bounded production-local purge
  passed: the targeted request is `processed` and the exact query returns zero.
  Host-level backup exclusion and physical chunk cleanup are not independently
  verified; the repository config contains no backup provider. A disposable
  restart diagnosis shows stale runtime state can outlive processing, while
  matching chunk objects remain under the configured physical-sweeper path.
  The seven-day telemetry canary remains open; its complete duration and
  packet-to-storage evidence are still open.
- The candidate branch is present on `origin`; no remote CI run covers this
  revision because the workflow runs on `main`, pull requests to `main`, or a
  manual dispatch. `session-handoff@0.7.2` is not published.
- Human benchmark review remains excluded by instruction.

No tag, publish, deploy, or spend was performed. The bounded production-local
test-row purge was performed and documented; no manual chunk deletion was
attempted.
