# session-handoff 0.7.1 release verdict

Date: 2026-09-02

Verdict: **NO-GO**. The local candidate and privacy/backend implementation are
ready for owner-gated validation, but four release gates remain open.

## Identifiable revisions

- Candidate worktree: `/home/cristina/session-handoff-release-0.7.1`
- Candidate branch: `release/0.7.1-candidate`
- Tested candidate content revision: `635f30f` (this report is a documentation-only successor)
- Backend worktree: `/home/cristina/selfhosted/telemetry`
- Backend revision: `35bc99d`
- Privacy/backend implementation: candidate `889b0e1`, `aa7add8`, `a321f24`;
  backend `02b5c32`, `4804188`, `68d918a`, `c5f7658`, `126794c`, `359c2ef`,
  `35bc99d`
- Benchmark audit trail: `49ce6d0` selection, `3593354` locked blind
  decisions, `635f30f` unblinded coverage/result

The backend retained all pre-existing dirty files. No reset, rewrite, real-data
purge, push, deploy, publish, remote tag, provider call, or spend occurred.

## Requirement evidence

| Requirement | Status | Evidence |
| --- | --- | --- |
| Curated aggregate threshold | Pass locally | Grafana/query/export cells require post-sum `aggregate_count >= 5`; raw private rows remain available for aggregation. This is not user k-anonymity and adds no identifier. |
| Allowlisted packet-to-storage equivalence | Pass locally | Disposable client-preview → Collector → Loki → Grafana tests cover field allowlists, tenant isolation, normalization and public packet variants. |
| Retention, backup and purge | Pass disposable; production open | Loki retention is `11232h`; no backup system is configured; random disposable Compose volumes were purged. Production evidence requires owner-authorized deletion. |
| Abuse controls and IP persistence | Pass locally | Gateway limit `30r/m`, burst `10`, `access_log off`; client IP is neither forwarded nor stored by the origin stack. |
| Independent privacy/security review | Pass locally | Independent root review found no unresolved blocking code/config finding after remediation. Cloudflare operational acceptance remains separate. |
| Public privacy notice | Pass | Controller/operator `privacy@yuzushi.party`; origin described as private self-hosted infrastructure in Italy, EU; Cloudflare global-edge processing disclosed without a home address or personal email. |
| CI readiness | Pass locally; remote open | Workflow covers Python 3.10/3.12, Ruff, pytest, compileall, manifests/package and Node 18 setup/MCP smoke. Remote execution requires a push. |
| Markdown default / structured state | Pass | Markdown remains default; structured state remains opt-in. |
| Blind benchmark calibration | Model coverage pass; human open | 36 decisions were committed before unblinding and cover 6 cases, 3 bands and all 4 conditions. `human_reviewed=false`, agreement `null`; a human comparison at agreement >= 0.8 is still required. |

## Fresh verification

- Candidate full suite: `680 passed, 2 skipped` on Python 3.12.3.
- Candidate Python 3.10 privacy/telemetry subset: `422 passed`; the full
  container run is limited by user-namespace restrictions in benchmark
  isolation.
- Candidate Ruff, compileall, package/manifest checks and Node 18 setup/MCP
  smoke: passed.
- Backend suite after final documentation alignment: `26 passed, 4 skipped`.
- Backend opt-in disposable integration: `3 passed` across packet equivalence,
  tenant isolation and public variants; teardown purged only random temporary
  volumes.
- Calibration validation: 36 immutable bundle hashes matched; coverage is 6
  cases, 3 bands, 4 conditions; focused benchmark tests: `38 passed`.
- Candidate/backend Compose config and `git diff --check`: passed.

Later candidate commits only add immutable calibration JSON and documentation;
they do not change the product code covered by the full candidate suite.

## Open gates and exact closure evidence

1. **Cloudflare edge acceptance:** owner explicitly accepts that the public
   tunnel uses Cloudflare's global edge, which may process connection IP and
   edge metadata under Cloudflare terms and an unasserted retention period.
2. **Remote CI:** owner authorizes pushing `release/0.7.1-candidate`; the remote
   workflow must finish green at the pushed revision.
3. **Seven-day production canary:** owner authorizes deployment/enrollment.
   The evidence must bind preview bytes to captured packet, Collector output,
   Loki labels and Grafana totals; show no persistent IP/access log; record
   rate-limit behavior, retention and an owner-authorized production purge.
4. **Human benchmark calibration:** an independent human scores the locked
   blind sample in `benchmark/calibration/2026-09-02-root-model-all-conditions.selection.json`.
   Record agreement, every disagreement and adjudication of critical ones;
   release requires agreement >= 0.8.

Re-evaluate GO only after all four evidence records are attached to the exact
candidate revision. Publishing, deploying and real deletion remain owner-only
actions.
