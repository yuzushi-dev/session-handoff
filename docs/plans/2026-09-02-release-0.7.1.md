# session-handoff 0.7.1 Release Preparation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Prepare a local, reproducible 0.7.1 candidate without publishing it.

**Architecture:** Carry the already-present telemetry/structured-state hardening and version-aware benchmark into one candidate commit, bump every package manifest together, and document evidence and explicit privacy/backend gates. Keep Markdown as the default and structured state opt-in.

**Tech Stack:** Python 3.10/3.12, pytest, Ruff, Node.js 18/npm, GitHub Actions, Markdown.

---

### Task 1: Establish release scope

**Files:**
- Modify: current tracked release-scope changes only.
- Include: `benchmark/version_aware.py`, `tests/test_version_aware.py`, three 2026-09-02 reports.
- Preserve: `.sando/`, `graphify-out/`, and unrelated plans/artifacts.

**Steps:**
1. Record baseline branch, commit, dirty paths, published refs, and package contents.
2. Create a local `release/0.7.1-candidate` branch from the current revision.
3. Keep non-release untracked artifacts out of the candidate commit.

### Task 2: Set patch-release metadata

**Files:** `package.json`, `plugin.json`, `.claude-plugin/plugin.json`, `.codex-plugin/plugin.json`.

**Steps:**
1. Change only the four manifest versions from `0.7.0` to `0.7.1`.
2. Add concise changelog/release notes naming the telemetry schema compatibility, benchmark tooling/reports, and the still-experimental structured-state path.
3. Add tests or update existing package assertions only if the metadata contract requires it.

### Task 3: Verify candidate behavior

**Steps:**
1. Run Ruff and the complete pytest suite on Python 3.10 and 3.12 environments when available.
2. Run compile checks, manifest validation, npm dry-run, package install, setup/doctor, and a real CLI/MCP smoke in isolated temporary homes.
3. Run the version-aware deterministic benchmark with the published 0.6.1/0.7.0 trees where available; do not run provider calls or publish.
4. Inspect CI workflow and report remote CI evidence separately from local runs.

### Task 4: Produce the audit handoff

**Files:** `/home/cristina/Obsidian/AgentMemory/10-projects/session-handoff.md`.

**Steps:**
1. Record candidate branch/commit/path, exact fresh commands/results, artifacts, GO/NO-GO, and owner-gated actions in one appended dated section via `obsidian-files.edit_file`.
2. Recheck candidate cleanliness, diff check, and no publish/deploy/tag mutation.
3. Report open privacy/backend gates and the provenance discrepancy around the already-published `v0.7.0`.
