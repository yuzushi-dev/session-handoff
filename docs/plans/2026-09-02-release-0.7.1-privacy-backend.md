# 0.7.1 Privacy/Backend Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Close the 0.7.1 privacy/backend gates with server-side aggregate suppression, reproducible disposable packet-to-storage evidence, and truthful release documentation without publishing or deploying.

**Architecture:** Keep the closed client schemas and Markdown/structured-state paths unchanged. Preserve allowlisted rows through Collector/Loki so independent rows can sum to five, then apply the minimum aggregate threshold after aggregation in curated dashboard/export queries. Use a disposable Compose stack plus an offline Python harness to prove allowlist, tenant, threshold, purge, rate-limit, and no-IP invariants. Document that `count` is an aggregate multiplier, not a distinct-user count, and that private Loki may retain rows below five for trusted operators.

**Tech Stack:** Python 3.10/3.12, pytest, Ruff, OpenTelemetry Collector, Loki, Grafana, nginx, Docker Compose, Markdown, Node.js 18/npm.

---

### Task 1: Record baseline and write RED tests

**Files:**
- Modify: `tests/test_telemetry_privacy.py`
- Modify: `/home/cristina/selfhosted/telemetry/tests/test_deploy.py`
- Create: `/home/cristina/selfhosted/telemetry/tests/test_disposable_privacy.py`

**Steps:**
1. Record both repository HEAD/status and existing user-owned dirty paths in the final evidence; do not reset or clean either tree.
2. Add failing tests requiring the Collector to preserve rows with `count` 1–4 for post-aggregation, retain only allowlisted attributes, and describe `count >= 5` as an aggregate threshold rather than distinct-user k-anonymity.
3. Add failing dashboard tests requiring every aggregate query to filter/return only `count >= 5` cells, while preserving structured-state and Markdown defaults.
4. Add failing disposable tests for packet-to-storage equivalence, unknown field rejection, retention/purge, no-backup declaration, `access_log off`, burst rejection, and absent IP in logs/metadata/dashboard output.
5. Run each new focused test and record the real RED failure before implementation.

### Task 2: Implement server-side threshold and disposable backend harness

**Files:**
- Modify: `/home/cristina/selfhosted/telemetry/otel-collector.yaml`
- Inspect (preserve existing owner changes): `/home/cristina/selfhosted/telemetry/loki.yaml`
- Modify: `/home/cristina/selfhosted/telemetry/nginx.conf`
- Modify: `/home/cristina/selfhosted/telemetry/docker-compose.yml`
- Modify: `/home/cristina/selfhosted/telemetry/grafana-dashboard-session-handoff.json`
- Modify: `/home/cristina/selfhosted/telemetry/grafana-dashboard-skill-state.json`
- Modify: `/home/cristina/selfhosted/telemetry/grafana-dashboard-sando.json`
- Modify: `/home/cristina/selfhosted/telemetry/grafana-dashboard-f2.json`
- Modify: `/home/cristina/selfhosted/telemetry/tests/test_deploy.py`
- Create: `/home/cristina/selfhosted/telemetry/tests/test_disposable_privacy.py`

**Steps:**
1. Keep the Collector allowlist/schema strict without dropping rows solely because `count` is below 5; curated post-aggregation queries reject cells below threshold while preserving rows that can sum to five. Do not add identifiers, dimensions, or providers.
2. Keep Collector resource and per-service attribute allowlists closed; verify disallowed attributes are dropped and unknown service/schema records never reach Loki.
3. Add post-aggregation threshold predicates to all dashboard/query expressions, including numeric unwrap paths, so cells with aggregate count below 5 cannot render; do not label this k-anonymity.
4. Keep Loki retention at 13 months and configure no backup provider; make internal storage semantics explicit: allowlisted aggregate rows and bounded labels/structured metadata remain private in Loki, while sub-threshold cells are omitted from curated query/export results and remain available to trusted private operators.
5. Ensure nginx has no access logging, bounded request/body handling, per-source burst rate limiting, and no IP forwarding into Collector/Loki metadata.
6. Add a provider-free disposable harness that exercises client-preview-shaped OTLP JSON through gateway/Collector/Loki query paths when Docker is available, otherwise runs deterministic config/payload/storage checks without creating persistent volumes.
7. Run the focused tests and record GREEN output.

### Task 3: Update privacy, threat, canary, and client docs/config evidence

**Files:**
- Modify: `docs/telemetry.md`
- Modify: `/home/cristina/selfhosted/telemetry/README.md`
- Modify: `/home/cristina/selfhosted/telemetry/docs/telemetry-privacy.md`
- Modify: `/home/cristina/selfhosted/telemetry/docs/telemetry-threat-model.md`
- Modify: `/home/cristina/selfhosted/telemetry/docs/telemetry-canary-report.md`
- Modify: `tests/test_telemetry_privacy.py`

**Steps:**
1. Remove privacy placeholders and state controller/operator `privacy@yuzushi.party`, self-hosted infrastructure in Italy/UE, purpose usage/performance, and no personal address/email.
2. State that `count >= 5` is a curated aggregate display/export threshold, not universal private-query authorization or user k-anonymity; no stable identifier exists and no unique-user denominator is computed.
3. Document local state, Collector memory, accepted Loki rows, retention, purge, no backups present, and disabled proxy access logs exactly as evidenced.
4. Document packet equivalence, rate-limit tunnel behavior, no-IP persistence, CI readiness, and owner-gated remote CI/review; preserve Markdown and structured-state defaults.
5. Run the client privacy/documentation tests and record GREEN output.

### Task 4: Fresh verification and separated commits

**Steps:**
1. Run focused client/backend tests, disposable integration tests if Docker is available, Ruff, full pytest on available Python 3.10/3.12 interpreters, Node 18 package/install/setup/MCP smoke, manifest/plugin validation, and `git diff --check`.
2. Re-run all tests from clean temporary homes/volumes as applicable; document unavailable environments and avoid provider calls.
3. Inspect diffs and stage only owned files. Create one identifiable client commit and one identifiable backend commit; never include benchmark outputs/judgments/calibration, pre-existing unrelated backend files, untracked secrets, or vault notes.
4. Report exact hashes, RED/GREEN commands/results, fresh verification limits, residual findings, and the owner action required for Cloudflare edge-data/retention acceptance, remote CI, the seven-day canary, and human benchmark review. Do not claim GO while those gates remain owner-gated.

### Recorded TDD checkpoints

- Client RED before the notice edits: `rtk pytest -q tests/test_telemetry_privacy.py` → 0 passed, 2 failed (missing contact/threshold contract).
- Backend RED before the gate edits: `rtk pytest -q tests/test_disposable_privacy.py` → 2 passed, 3 failed (missing post-aggregation dashboard threshold/no-backup evidence).
- Architecture correction: no ingress filter drops `count < 5`; five `count=1` rows remain available for a private Loki sum. Suppression is only in post-aggregation query/dashboard expressions.
- Independent review RED: the old public receiver/pipeline allowed a public
  request to write `skill-state-runtime`, and the pre-allowlist Collector path
  accepted secret-shaped bodies/arbitrary values; the first strict config also
  failed to start because this pinned Collector has no `IsEmpty` OTTL function.
- Remediation GREEN: pinned Collector config starts; real session-handoff
  payload survives, hostile body/allowed-key values and OTLP channels do not
  reach Loki, public 4318 cannot write loopback tenants, local 4319 still does,
  and all five public Sando serializer variants reach the Sando tenant.
- Focused GREEN after implementation: client privacy tests 55 passed; backend privacy/deploy tests 20 passed, 2 skipped; opt-in disposable packet-to-storage integration 1 passed in 24.04s.
- Follow-up RED: delaying the fake interactive host by 0.5s before
  `tty.setraw` reproduced the PTY draft-loss hang; the old fixed +0.25s write
  reached canonical input and was discarded by `TCSAFLUSH`.
- Follow-up GREEN: `_DraftProcess` now waits for the PTY slave to leave
  canonical mode before writing the draft; the delayed-host regression and
  all 33 session-switch tests pass.
- Follow-up documentation gate: the public path is Cloudflare Tunnel
  `sando-telemetry` on the `yuzushi.party` Free Website zone, origin
  `http://gateway:4318`, with no account/zone Logpush job. Cloudflare edge
  processing and retention remain owner acceptance gates; no retention period
  is asserted. Remote CI, the seven-day canary, and human benchmark review
  also remain open.
