apiVersion: 1
datasources:
  - name: Loki — session-handoff
    uid: loki-session-handoff
    type: loki
    access: proxy
    url: http://loki:3100
    jsonData:
      httpHeaderName1: X-Scope-OrgID
    secureJsonData:
      httpHeaderValue1: session-handoff
  - name: Loki — sando
    uid: loki-sando
    type: loki
    access: proxy
    url: http://loki:3100
    jsonData:
      httpHeaderName1: X-Scope-OrgID
    secureJsonData:
      httpHeaderValue1: sando
