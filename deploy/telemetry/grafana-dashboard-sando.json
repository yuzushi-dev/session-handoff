{
  "title": "Sando Telemetry",
  "schemaVersion": 39,
  "version": 1,
  "tags": ["sando", "aggregate", "opt-in"],
  "templating": {"list": []},
  "panels": [
    {
      "id": 1,
      "type": "timeseries",
      "title": "Hook and proxy activity",
      "datasource": {"type": "loki", "uid": "loki-sando"},
      "targets": [{"expr": "sum by (event) (count_over_time({service_name=\"sando\"} | json [1d]))", "queryType": "range"}],
      "fieldConfig": {"defaults": {}, "overrides": []}
    },
    {
      "id": 2,
      "type": "text",
      "title": "Sampling notice",
      "options": {"mode": "markdown", "content": "Telemetry is opt-in aggregate data and may not represent all users."}
    }
  ]
}
